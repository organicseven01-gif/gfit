import { EscolherControle } from "@/components/painel/escolher-controle";

export const metadata = { title: "Controle pelo Celular" };

export default function ControlePage() {
  return <EscolherControle />;
}
